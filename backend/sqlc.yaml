version: "2"

sql:
  - engine: "postgresql"   
    schema: "schema/products"
    queries: "queries/products"
    gen:
      go:
        package: "productsdb"
        out: "internal/db/products"
        emit_interface: true
        #sql_package: "database/sql"  
  - engine: "postgresql"   
    schema: "schema/users"
    queries: "queries/users"
    gen:
      go:
        package: "usersdb"
        out: "internal/db/users"
        emit_interface: true
  - engine: "postgresql"   
    schema: "schema/payments"
    queries: "queries/payments"
    gen:
      go:
        package: "paymentsdb"
        out: "internal/db/payments"
        emit_interface: true
     